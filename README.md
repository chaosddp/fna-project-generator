Personal FNA + Nez project generator, since it need too many steps to set up a FNA project. 

It will create a solution and a game project, then clone FNA (with binary dependencies) and Nez by default.

